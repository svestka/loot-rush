{
  "name": "loot-drop-demo",
  "version": "2.0.0",
  "description": "LootRush - DevOps demo: race conditions & deadlocks with New Relic observability",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node db/migrate.js",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "test": "node tests/game.test.js",
    "lint": "node --check server.js && node --check routes/game.js && node --check db/pool.js && node --check db/seed.js && node --check db/migrate.js && node --check logger.js && node --check newrelic.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "newrelic": "^12.0.0",
    "pg": "^8.13.0",
    "ws": "^8.16.0"
  }
}
